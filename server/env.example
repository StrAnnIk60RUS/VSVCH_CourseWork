# Copy to .env in this folder. One initial migration (init-schema.sql): npm run db:migrate -w server (from repo root).
# If you had older migration names in SequelizeMeta, clear them or recreate the DB before migrating. Seed: npm run db:seed -w server.
DATABASE_URL="postgresql://user:password@localhost:5432/vsvh_languages?schema=public"
JWT_SECRET="change-me-in-production"
PORT=4000
CLIENT_ORIGIN="http://localhost:5173"
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM="noreply@vsvh.local"
